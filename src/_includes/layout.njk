<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <style>
    body { display: flex; margin: 0; font-family: Arial, sans-serif; min-height: 100vh; scroll-behavior: smooth; }
    nav { width: 250px; background-color: #333; color: #fff; padding: 1em; height: 100vh; }
    .language-toggle {
      display: flex;
      align-items: center;
      gap: 0.5em;
      margin-bottom: 1em;
    }
    .language-toggle a { color: #ddd; text-decoration: none; }
    .language-toggle a:hover { color: #fff; }
    nav a { color: #ddd; display: block; padding: 0.5em 0; text-decoration: none; }
    nav a:hover { color: #fff; }
    .content { padding: 2em; flex-grow: 1; }
  </style>
</head>
<body>
  <nav>
    <div class="language-toggle">
      <a href="/en/">English</a> | <a href="/ja/">日本語</a>
    </div>
    <a href="#about">{{ nav[lang].about }}</a>
    <a href="#activities">{{ nav[lang].activities }}</a>
    <a href="#members">{{ nav[lang].members }}</a>
    <a href="#links">{{ nav[lang].links }}</a>
    <a href="#contact">{{ nav[lang].contact }}</a>
  </nav>
  <main class="content">
    <h1>{{ title }}</h1>
    {{ content | safe }}
  </main>
</body>
</html>
